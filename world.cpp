//World handler

#include <raylib.h>
#include "blocks.h"
#include "consts.h"
#include "world.h"

void World::update(float dt) {
    for (int i = 0; i < SCRN_W; i++) {
        //rendering call from blocks.h
    }

    for (int i = 0; i < SCRN_H; i++) {
        //rendering call from blocks.h
    }
}

void gen_terrain() {
    return;
}